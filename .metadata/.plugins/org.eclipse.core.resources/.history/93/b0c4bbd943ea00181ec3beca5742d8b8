spring.application.name=currency-conversion-service
server.port=8100


#naming list of currency exchange server instances for ribbon
# now every time an instance is created we have to mention it here
#in order to avoid that we can use "Netflix-Eureka-Server" to register/de-register instances dynamically
currency-exchange-service.ribbon.listOfServers=http://localhost:8000,http://localhost:8001